{
  "name": "stockapp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n api,web,admin -c blue,green,yellow \"npm run dev --workspace=packages/api\" \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/admin\"",
    "build": "npm run build --workspace=packages/api && npm run build --workspace=apps/web && npm run build --workspace=apps/admin",
    "test": "npm run test --workspace=packages/api",
    "db:generate": "npm run db:generate --workspace=packages/db",
    "db:migrate": "npm run db:migrate --workspace=packages/db",
    "db:seed": "npm run db:seed --workspace=packages/db",
    "db:studio": "npm run db:studio --workspace=packages/db",
    "db:reset": "npm run db:reset --workspace=packages/db"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5",
    "turbo": "^2.9.16",
    "typescript": "^5.3.3"
  },
  "overrides": {
    "semver": "^7.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
